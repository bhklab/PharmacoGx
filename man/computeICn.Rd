% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/computeIC50.R, R/computeICn.R
\name{computeIC50}
\alias{computeIC50}
\alias{computeAC50}
\alias{computeACn}
\alias{computeICn}
\title{Computes inhibition concentrations for a drug dose viability curve}
\usage{
computeIC50(
  concentration,
  viability,
  Hill_fit,
  reference = c("relative", "absolute"),
  conc_as_log = FALSE,
  viability_as_pct = TRUE,
  verbose = TRUE,
  trunc = TRUE
)

computeAC50(
  concentration,
  viability,
  Hill_fit,
  conc_as_log = FALSE,
  viability_as_pct = TRUE,
  verbose = TRUE,
  trunc = TRUE
)

computeACn(
  concentration,
  viability,
  Hill_fit,
  n,
  conc_as_log = FALSE,
  viability_as_pct = TRUE,
  verbose = TRUE,
  trunc = TRUE
)

computeICn(
  concentration,
  viability,
  Hill_fit,
  n,
  reference = c("relative", "absolute"),
  conc_as_log = FALSE,
  viability_as_pct = TRUE,
  verbose = TRUE,
  trunc = TRUE
)
}
\arguments{
\item{concentration}{\code{numeric} is a vector of drug concentrations.}

\item{viability}{\code{numeric} is a vector whose entries are the viability values observed in the presence of the
drug concentrations whose logarithms are in the corresponding entries of conc, where viability 0
indicates that all cells died, and viability 1 indicates that the drug had no effect on the cells.}

\item{Hill_fit}{\code{list} or \code{vector} In the order: c("Hill Slope", "E_inf", "EC50"), the parameters of a Hill Slope
as returned by logLogisticRegression. If conc_as_log is set then the function assumes logEC50 is passed in, and if
viability_as_pct flag is set, it assumes E_inf is passed in as a percent. Otherwise, E_inf is assumed to be a decimal,
and EC50 as a concentration.}

\item{reference}{\code{character(1)} Whether inhibition should be measured
relative to the fitted dynamic range (\code{"relative"}, default) or against an
absolute viability threshold (\code{"absolute"}). The absolute-threshold mode is
deprecated in favour of \code{computeACn()}.}

\item{conc_as_log}{\code{logical}, if true, assumes that log10-concentration data has been given rather than concentration data,
and that log10(ICn) should be returned instead of ICn.}

\item{viability_as_pct}{\code{logical}, if false, assumes that viability is given as a decimal rather
than a percentage, and that E_inf passed in as decimal.}

\item{verbose}{\code{logical}, if true, causes warnings thrown by the function to be printed.}

\item{trunc}{\code{logical}, if true, causes viability data to be truncated to lie between 0 and 1 before
curve-fitting is performed.}

\item{n}{\code{numeric} The inhibition level to compute. If \code{viability_as_pct = TRUE} it is treated as a percent inhibition; otherwise it is assumed to be
a decimal fraction.}
}
\value{
\code{numeric(1)} The IC50 of the Hill curve over the specified dose
range.

\code{numeric(1)} The AC50 of the Hill curve over the specified dose
range.

a numeric value for the concentration of the requested inhibition
level.
}
\description{
Returns the concentration corresponding to the requested percent inhibition.
By default, inhibition is measured relative to the fitted dynamic range of the
curve, so \code{computeIC50()} returns the half-maximal inhibitory concentration.
Absolute-threshold helpers \code{computeACn()} and \code{computeAC50()} are provided for
the historical "absolute viability" behaviour. Use \code{computeAC50()}, not the
default \code{computeIC50()}, when comparing against published AC50 values that
represent an absolute 50\% viability threshold.
}
\section{Functions}{
\itemize{
\item \code{computeIC50()}: Returns the relative IC50 of a drug dose-response
curve. Use \code{computeAC50()} for an absolute 50\% viability threshold.

\item \code{computeAC50()}: Returns the AC50 of a drug dose-response curve using
an absolute 50\% viability threshold. This is the appropriate helper for
comparisons against published AC50 values.

\item \code{computeACn()}: Returns the ACn of a drug dose response curve using an
absolute viability threshold.

}}
\examples{
dose <- c(0.0025, 0.008, 0.025, 0.08, 0.25, 0.8, 2.53, 8)
viability <- c(108.67, 111, 102.16, 100.27, 90, 87, 74, 57)

# Relative half-maximal inhibition concentration.
computeIC50(dose, viability)

# Absolute 50\% viability threshold, suitable for published AC50 comparisons.
computeAC50(dose, viability)

computeICn(dose, viability, n = 10)

}
